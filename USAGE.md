# 🐍 Python 技能图谱可视化系统 - 使用指南

## 🚀 快速开始

### 方法一：使用启动脚本（推荐）
```bash
./start.sh
```

### 方法二：手动启动
```bash
# 1. 安装依赖
pip install Flask pandas openpyxl flask-cors werkzeug

# 2. 启动应用
python3 app.py
```

## 📱 访问应用

启动成功后，在浏览器中访问：**http://localhost:5000**

## 🎯 功能演示

### 1. 默认可视化
- 应用启动后会自动加载Python技能图谱数据
- 显示Python及其相关技能的关系网络
- 节点大小表示重要程度，颜色表示不同分类

### 2. 交互操作

#### 🖱️ 鼠标操作
- **拖拽节点**：点击并拖拽任意节点来重新排列
- **悬停查看**：鼠标悬停在节点上查看详细信息
- **缩放视图**：使用鼠标滚轮缩放图谱

#### 🔍 搜索功能
1. 在左侧控制面板的搜索框中输入技能名称
2. 匹配的节点会高亮显示
3. 支持中文搜索，如搜索"基础"、"编程"等

#### 🎛️ 显示控制
- **显示/隐藏标签**：切换节点文字标签的显示
- **显示/隐藏连线**：控制节点间连接线的显示
- **调节连接强度**：使用滑块调整力导向图的连接强度

### 3. 文件导入功能

#### 📁 支持的文件格式

**JSON 格式示例：**
```json
{
  "nodes": [
    {
      "id": "Python",
      "group": "core",
      "size": 40,
      "color": "#dc3545"
    }
  ],
  "links": [
    {
      "source": "Python",
      "target": "基础语法"
    }
  ]
}
```

**Excel 格式要求：**
- 文件需包含两个工作表：`nodes` 和 `links`
- `nodes` 表列：id, group, size, color
- `links` 表列：source, target

#### 📤 导入步骤
1. 点击右上角"📁 导入数据"按钮
2. 选择 JSON 或 Excel 文件
3. 点击"上传"按钮
4. 系统会自动解析并更新可视化

#### 📥 下载示例
点击"📥 下载示例"按钮可获取标准格式的 Excel 模板文件。

## 🎨 自定义数据

### 节点属性说明
| 属性 | 说明 | 示例 |
|------|------|------|
| id | 唯一标识符 | "Python" |
| group | 分组名称 | "core" |
| size | 节点大小 | 40 |
| color | 节点颜色 | "#dc3545" |

### 颜色建议
- 核心技能：`#dc3545` (红色)
- 基础技能：`#17a2b8` (蓝色) 
- 进阶技能：`#6c757d` (灰色)
- 专业技能：`#28a745` (绿色)
- 工具技能：`#ffc107` (黄色)

## 🔧 故障排除

### 常见问题

**1. 应用无法启动**
- 检查 Python 版本（需要 3.7+）
- 确保所有依赖已正确安装
- 检查端口 5000 是否被占用

**2. 文件上传失败**
- 确认文件格式正确（JSON/Excel）
- 检查文件大小（限制16MB）
- 验证数据结构是否符合要求

**3. 可视化显示异常**
- 尝试点击"🔄 重置视图"按钮
- 检查浏览器控制台是否有错误信息
- 确认网络连接正常

### 性能建议
- 建议节点数量控制在100个以内
- 大数据集建议分组展示
- 定期清理上传的文件

## 🛠️ 技术支持

### 系统要求
- Python 3.7+
- 现代浏览器（Chrome、Firefox、Safari、Edge）
- 至少 512MB 可用内存

### 依赖包版本
- Flask 3.1+
- Pandas 2.0+
- OpenPyXL 3.1+
- D3.js 7.0+

### 端口配置
默认端口：5000
如需修改，编辑 `app.py` 文件中的端口设置。

## 📞 获取帮助

遇到问题时，请检查：
1. 浏览器开发者工具的控制台错误
2. 终端中的应用日志信息
3. 确认文件格式和数据结构正确性

---

**提示**：建议在使用前先用示例数据测试功能，熟悉操作后再导入自己的数据。